source("hedge.R")
library(readr)
fxdata <- read_csv("data/fxdata.csv")
x <-fxdata

# obtain the hr and he when WinLen =15
mvhr <- function(x, WinLen=15)


  ### to do

  data format 바꾸기
  r-cmd --> push ---> badge
  testthat::
  readme file 바꾸기
  citation??
